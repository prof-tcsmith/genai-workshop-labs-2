# Copy to secrets.toml (gitignored) to set a default key for local `streamlit run`.
# For Docker, prefer live-demos/.env instead. Attendees can always paste a key in the UI.
# openai_api_key = "sk-proj-your-key-here"
