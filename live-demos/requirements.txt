# Touching this file forces Streamlit Cloud to rebuild the environment (a full
# full process restart) - needed when shared/ modules change, because hot-reload
# re-runs app.py against the OLD shared.core still cached in sys.modules.
streamlit>=1.38
openai>=1.40
anthropic>=0.40
numpy>=1.26
mcp>=1.2
pinecone>=5.0
# rebuild-stamp: v1.4.4 — try_this() added to shared/core (2026-07-18)

# cache-bust: 2026-07-22c chunker overlap cap + embed batching (Lab 5 tiny chunks fix)
