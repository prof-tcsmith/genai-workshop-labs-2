# Copy to .env (gitignored) and paste the workshop OpenAI key.
# `docker compose` reads .env automatically and passes it into the live-demos container.
# (You can also skip this and paste the key in the app sidebar instead.)
OPENAI_API_KEY=sk-proj-your-key-here
